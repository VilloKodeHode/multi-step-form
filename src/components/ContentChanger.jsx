import { createContext } from "react";

const PageIs = createContext();
export const PersonalInfo = createContext();
export const PlanChoice = createContext();
export const SliderChoice = createContext();
export const AddOnChoice = createContext();

export default PageIs;
